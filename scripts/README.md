
Claude Code Usage Monitor (prototype)

1. install dependencies
pip install streamlit pandas

2. generate mock usage data
python collector.py

3. launch dashboard
streamlit run dashboard.py

Future idea:
- connect Claude Code hooks
- log real tokens
- per‑issue cost analytics
